#!/bin/sh
# 解析符号链接（npm bin 会把它链接到 node_modules/.bin/）
SELF="$0"
if command -v readlink >/dev/null 2>&1; then
  RESOLVED=$(readlink -f "$SELF" 2>/dev/null) && SELF="$RESOLVED"
fi
exec node --disable-warning=ExperimentalWarning "$(dirname "$SELF")/../src/cli.ts" "$@"
